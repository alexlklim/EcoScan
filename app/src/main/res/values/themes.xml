<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcoScan" parent="Theme.Material3.DayNight.NoActionBar">
         <item name="colorPrimary">@color/buttons</item>
         <item name="android:textSize">15sp</item>
    </style>


    <style name="btnStyle" parent="Widget.Material3.Button">
        <item name="android:textSize">20sp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:background">@drawable/btn_rounded</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <!-- Add other button-related attributes here if needed -->
    </style>

    <style name="btnSettings" parent="Widget.Material3.Button">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">center</item>
        <item name="background">@drawable/btn_square</item>
        <!-- Add other button-related attributes here if needed -->
    </style>

    <style name="simpleText" parent="Widget.Material3.Button">
        <item name="android:height">60dp</item>
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="simpleEditText" parent="Widget.Material3.Button">
        <item name="android:height">60dp</item>
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:ems">10</item>
        <item name="android:inputType">number</item>
    </style>

    <style name="Theme.EcoScan" parent="Base.Theme.EcoScan" />
</resources>

<!--    <solid android:color="#90FFFFFF" /> -->